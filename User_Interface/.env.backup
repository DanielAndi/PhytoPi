# PhytoPi Dashboard Environment Variables
# Copy this file to .env for your configuration
# Do NOT commit .env to version control (it's in .gitignore)

# ============================================================================
# Supabase Configuration
# ============================================================================

# Option 1: Local Development (Supabase running locally)
# Use this when running Supabase with: cd infra/supabase && supabase start
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=your-local-anon-key-here
# Get your local anon key with: cd infra/supabase && supabase status

# Option 2: Staging/Production (Remote Supabase)
# Uncomment and set these for staging or production builds:
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your-production-anon-key-here

# Option 3: Local Supabase for Android Testing (use your computer's IP)
# If testing Android on a device, use your computer's local IP instead of 127.0.0.1
# Find your IP with: ip addr show | grep "inet " | grep -v 127.0.0.1
# SUPABASE_URL=http://192.168.1.XXX:54321  # Replace XXX with your IP
# SUPABASE_ANON_KEY=your-local-anon-key-here

# Android SDK Configuration (optional, auto-detected if installed)
# ANDROID_HOME=/opt/android-sdk
# ANDROID_SDK_ROOT=/opt/android-sdk

# API Configuration
API_BASE_URL=http://127.0.0.1:54321

# App Configuration
APP_NAME=PhytoPi Dashboard
APP_VERSION=1.0.0

